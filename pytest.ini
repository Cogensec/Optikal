[pytest]
testpaths = tests
addopts =
    --tb=short
    --strict-markers
    -v
markers =
    slow: marks tests as slow (skip with -m "not slow")
    integration: marks tests that require a full training run
